import { CopilotRuntime, copilotRuntimeNextJSAppRouterEndpoint } from '@copilotkit/runtime'; import { LangGraphHttpAgent } from '@copilotkit/runtime/langgraph'; const agent = new LangGraphHttpAgent({ url: 'http://localhost:8000/copilotkit' }); const runtime = new CopilotRuntime({ agents: [agent], a2ui: { injectA2UITool: true } }); const handler = copilotRuntimeNextJSAppRouterEndpoint({ runtime, endpoint: '/api/copilotkit' }); handler.handleRequest(new Request('http://localhost:3000/api/copilotkit/info', { method: 'POST', body: '{}' })).then(res => res.json()).then(console.log).catch(console.error)
